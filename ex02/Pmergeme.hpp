#ifndef PMERGEME_HPP
# define PMERGEME_HPP

class Pmergeme
{
private:
    /* data */
public:
    Pmergeme();
    PmergeMe (const PmergeMe &ref)
    PmergeMe & operator = (const PmergeMe &ref);
    ~Pmergeme();
};

#endif